include the variable class in the specific psf model file, so the zernike variables class definition can be a part of the zernike model

include the varinfo in the variables, varinfo is just if its shared or not, then only one object has to be passed to the optimizer and everything is in one place

